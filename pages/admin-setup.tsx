import Head from "next/head";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { signUp } from "../lib/adminRepository";
export default function AdminSetup(){const[message,setMessage]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);setError("");const data=new FormData(e.currentTarget);try{const result=await signUp(String(data.get("email")),String(data.get("password")));setMessage(result.session?"Аккаунт создан. Теперь можно войти.":"Проверьте почту и подтвердите регистрацию.");}catch(err){setError(err instanceof Error?err.message:"Не удалось создать аккаунт");}finally{setLoading(false)}};return <><Head><title>Создание аккаунта — Atlas</title><meta name="robots" content="noindex,nofollow"/></Head><div className="admin-shell"><header className="admin-header"><Link href="/" className="brand"><span className="brand-mark">A</span><span>Atlas</span></Link><span className="admin-badge">Настройка администратора</span></header><main className="login-wrap"><section className="login-card"><p className="eyebrow">Первый вход</p><h1>Создать аккаунт</h1><p>Доступ уже разрешён только для указанного email.</p><form onSubmit={submit}><label htmlFor="setup-email">Email</label><input id="setup-email" name="email" type="email" defaultValue="gogachij@gmail.com" autoComplete="email" required/><label htmlFor="setup-password">Новый пароль</label><input id="setup-password" name="password" type="password" minLength={8} autoComplete="new-password" required/><button className="primary" type="submit" disabled={loading}>{loading?"Создаём…":"Создать аккаунт"}</button></form>{message&&<><p className="form-success" role="status">{message}</p><Link href="/admin">Перейти ко входу →</Link></>}{error&&<p className="form-error" role="alert">{error}</p>}</section></main></div></>}
